MYSTEEL_USERNAME=your_mysteel_username
MYSTEEL_PASSWORD=your_mysteel_password

# Default download directory for exported Excel files.
MYSTEEL_DOWNLOAD_DIR=E:\code\steel_price\data

# Optional: clear old exported files in the download directory before each full run.
MYSTEEL_CLEAR_DOWNLOAD_DIR=true

# Optional: explicit Chrome/Chromium executable path.
# Leave empty to use built-in fallback detection.
MYSTEEL_CHROME_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe

# Set to true to fill the page date inputs using env values below.
MYSTEEL_MANUAL_DATE=false
MYSTEEL_STARTDATE=2026-04-01
MYSTEEL_ENDDATE=2026-04-01

# Set to true to allow runs on weekends or holidays.
MYSTEEL_FORCE_RUN_NON_WORKDAY=false

# Random startup delay before the script begins running.
# WeCom / Enterprise WeChat settings.
# Preferred: point this to the basic_code repository root that contains wechat.py.
# Example on one machine: E:\code\basic_code
# Example on another machine: E:\jerry\code\basic_code
WECHAT_CODE_DIR=E:\code\basic_code

# Optional fallback only if WECHAT_CODE_DIR is not set.
PYTHONPATH=
WX_CORP_ID=your_corp_id
WX_AGENT_ID=your_agent_id
WX_SECRET=your_secret
WX_ROBOT_WEBHOOK=

# Recipients used by scripts\send_wechat_files.py
# Separate multiple users with comma, semicolon, or |
WECHAT_TOUSERS=user_a|user_b
WECHAT_DEFAULT_FILE=E:\code\steel_price\data\Total_Price.xlsx

# MariaDB settings used by test\汇总历史数据.py
MARIADB_HOST=127.0.0.1
MARIADB_PORT=3307
MARIADB_USER=root
MARIADB_PASSWORD=your_mariadb_password
MARIADB_DATABASE=steel_price
MARIADB_TABLE=total_price_history

